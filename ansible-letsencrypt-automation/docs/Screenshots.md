# Screenshots

> Placeholder for portfolio screenshots. Replace with real captures before publishing.

- `ansible-playbook --check --diff` dry run output
- Successful `ansible-lint` / `yamllint` CI run (GitHub Actions tab)
- `molecule test` passing output
- `curl -vI https://example.com` showing the issued certificate and HSTS header
