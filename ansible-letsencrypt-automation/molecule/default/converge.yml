---
- name: Converge
  hosts: all
  become: true

  pre_tasks:
    - name: Install NGINX so certbot's --nginx plugin has something to manage
      ansible.builtin.apt:
        name: nginx
        state: present
        update_cache: true

  roles:
    - role: certbot
